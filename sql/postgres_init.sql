CREATE SCHEMA EVENT_STORE;

CREATE TABLE EVENT_STORE.EVENT_NOTIFICATION(
    EVENT_ID SERIAL PRIMARY KEY,
    HMPPS_ID varchar(15),
    EVENT_TYPE varchar(20),
    URL varchar(200),
    LAST_MODIFIED_DATETIME TIMESTAMP NOT NULL
);